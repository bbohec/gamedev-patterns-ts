/*
import { describe, beforeEach, it } from 'mocha'
import chai = require('chai');
import spies = require('chai-spies')
import { Canvas } from '../utils'
import { CanvasLayer } from '.'
chai.use(spies)
const expect = chai.expect
// jest.mock('@/utils')
chai.spy.on()
describe('>>> CanvasLayer', () => {
    it('should create canvas only once', () => {
        expect(Canvas).not.to.be.called()
        const canvas1 = CanvasLayer.Background
        const canvas2 = CanvasLayer.Background
        expect(canvas1).to.be.deep.equal(canvas2)
        expect(Canvas).to.be.called.exactly(1)
    })
})
*/
